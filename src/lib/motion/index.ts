/**
 * Motion Utilities
 *
 * Central export point for all animation utilities.
 * Import everything you need for animations from this single file.
 *
 * @module motion
 *
 * @example
 * ```tsx
 * import {
 *   fadeInUp,
 *   staggerContainer,
 *   useScrollReveal,
 *   duration,
 *   easing
 * } from '@/lib/motion';
 * ```
 */

// Tokens
export * from './tokens';

// Variants
export * from './variants';

// Hooks
export * from './hooks';
